# RECRUIT.AI Backend Dependencies
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
# MongoDB. Beanie is the ODM; motor is its async driver.
#
# Pinned below 2.0: Beanie 2.x calls list_collection_names(authorizedCollections=…),
# which mongomock-motor does not implement, and that is what lets the test
# suite run real queries without a MongoDB server. Revisit when mongomock-motor
# supports it.
beanie>=1.29,<2.0
motor>=3.5.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
python-multipart>=0.0.9
pydantic[email]>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1
qrcode[pil]>=7.4.2
boto3>=1.34.0
# Reserved for moving rate-limit counters out of process memory. Not yet
# imported by the application; see the rate limiting note in README.
redis>=5.0.0
httpx>=0.27.0
# Optional: only used when SENTRY_DSN is set.
sentry-sdk[fastapi]>=2.0.0
aiofiles>=24.1.0
python-dateutil>=2.9.0
